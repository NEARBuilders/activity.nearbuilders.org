---
"api": minor
---

Add Source API Key-authenticated, exactly-once Activity event submission with durable idempotency and relay acknowledgement handling. Keep local relay selection configurable at runtime and disable incompatible optional WebSocket native accelerators in API bundles.
